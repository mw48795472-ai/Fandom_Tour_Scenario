# RegionFit 모형

지역 `r` 과 팬덤 `f` 의 조합에 점수를 매긴다. 산출 불가 항은 제외하고 남은 가중치로 재정규화한다.

```
RegionFit(f, r) = 0.30 · 연고밀착도
                + 0.25 · 업종정합도
                + 0.20 · 파급효과
                + 0.086 · 상업유발도
                + w_L  · 해외확산도        (0.005 ~ 0.014, 페르소나별)
                + w_V  · 공연 수용력        (0.10 또는 0.20, 페르소나별)
                − 0.10 · 집중리스크
```

가중치 중 **데이터로 정한 것은 상업유발도와 해외확산도 둘뿐**이다. 나머지는 설계값이며
그 이유는 [VALIDATION.md](VALIDATION.md) 의 "검정할 수 없는 항" 절에 적었다.

---

## 1. 연고밀착도 — 두 채널의 기하평균

증거가 두 종류다. **담론 채널**(코퍼스에 그 지역이 언급됨)과 **실연 채널**(실제로 그 지역에서 공연함).
같은 "지역과 연결된다"라도 성격이 달라 합산하지 않고 기하평균으로 묶는다.

### 채널별 LQ

```
사후 점유(f, r) = ( n + α_r ) ÷ ( N + α )         α = Σ α_r
지역 LQ(f, r)   = 사후 점유 ÷ π_r                  π_r = α_r / α
```

`α_r` 은 **비대칭 디리클레-다항의 최대우도 추정값**이다. 17개 시도에 균등 배분(α/17)하지 않는 이유는
전국 언급의 39.9%가 서울에 몰려 있어서다 — 균등 사전을 쓰면 100개 중 76개의 대표지역이 서울로 잡히는데,
그건 연고가 아니라 기저율이다.

| 채널 | 총 농도 α | 서울 기저 π | 대칭 사전 대비 ΔlogL |
|---|---|---|---|
| 담론 (코퍼스 언급) | 13.79 | 42.1% | +312.4 (LR p < 10⁻¹⁰⁰) |
| 실연 (공연 이력) | 14.01 | 52.2% | +313.9 |

### 결합과 정규화

```
결합 LQ = √( 담론 LQ × 실연 LQ )       두 채널 모두 산출될 때
        = 산출되는 쪽 단독               한쪽이 불가할 때

A(f, r) = clip( log(결합 LQ) ÷ log(22.27), 0, 1 )
```

기하평균이라 **한 채널만 높으면 끌려 내려가고, 두 채널이 같은 지역을 가리킬 때만 값이 유지된다.**
로그 정규화는 LQ = 1(전국 기저 수준)에서 정확히 0이 되도록 맞춘 것이다. 22.27은 표본 최대.

### 측정 해상도

두 채널 모두 **시도 단위**다. 시군구를 골라도 같은 값을 쓰고, 멤버 연고가 그 시군구를 지목하는
후보만 앞세운다. v0.5까지는 시도 값을 시군구 수로 나눴는데, 그 상수는 그 지역의 모든 후보에
똑같이 곱해져 순위 정보가 없으면서 연고 항만 1/22로 줄여 항 균형을 무너뜨렸다 — 제거했다.

---

## 2. 업종정합도 — 지역 × 아티스트

지역 한쪽만 보던 항을 v0.7에서 양쪽으로 바꿨다.

```
아티스트 업종 LQ(f, 영역) = ( 광고 건수 + α_영역 ) ÷ ( 총 + κ ) ÷ π_영역     κ = 7.35

혼합 가중(f, 영역) ∝ 페르소나 템플릿(영역) × 아티스트 업종 LQ(f, 영역)      → 합 1로 재정규화

B(f, r) = Σ 혼합 가중 × min( 지역 소비 LQ ÷ 2, 1 )
```

광고 근거가 없는 팬덤은 아티스트 LQ가 전 영역에서 **정확히 1.0**이 되므로 혼합 가중이
페르소나 템플릿과 같아진다 — 즉 이전 버전과 동일한 값으로 되돌아간다.

**업종 매핑** (광고 20업종 → 카드소비 5영역, 커버리지 51.1%)

| 소비영역 | 광고 업종 |
|---|---|
| 음식 | 식음료 |
| 관광/쇼핑 | 쇼핑 · 패션/의류 · 미용 |
| 레저활동 | 스포츠/레저 |
| 공연/전시 | 엔터테인먼트 |
| 숙박 | 여행 |
| — (대응 없음) | 복지/행정 · 금융 · 자동차 · 정보/통신 · 건강/의료 · 가정/생활 · 비즈니스/산업 · 부동산 · 교육 · 게임 · 뉴스 · 도서/참고자료 · 기타 |

대응 없는 업종을 억지로 끼우면 방향 신호가 아니라 잡음이 된다. 다만 그중 **복지/행정이 단일 최대
업종(15.3%)** 이고 내용상 지자체 위촉·공익 캠페인이라, 점수에는 넣지 않고 협업 이력으로 따로 표기한다.

---

## 3. 상업유발도 — 강도

```
축소 광고비중(f) = ( 광고성문장수 + α_C · m ) ÷ ( 근거문장수 + α_C )
                   α_C = 21.82,  m = 0.1221         ← 베타-이항 최대우도

C(f) = 축소 광고비중 ÷ 0.3502                        0.3502 = 표본 최대 (이효리)
```

α_C 가 큰 이유는 광고 태깅이 희소해 팬덤 간 관측 분산이 이항 잡음에 가깝기 때문이다 —
자료가 강하게 축소하라고 말한다. 그래서 광고 0건인 김동률·나훈아도 0이 아니라 사전평균 근처에 놓인다.

**이 항만 검정을 통과했다** (편상관 +0.512, ΔR² +0.049). 다만 파급효과와 원상관 +0.589로
겹치므로 두 항을 독립 근거로 읽으면 안 된다.

### 광고효과를 소비 금액으로 환산

점수와 별개로, 광고가 어느 업종에 얼마를 겨냥하는지를 실제 카드소비액으로 표기한다.

```
겨냥 소비 기반(f, r) = Σ_영역  아티스트 광고 구성비(f, 영역) × 카드소비액(r, 영역)
```

예: BTS × 경기 = 34.7조원 (경기 전체 소비 159.4조의 21.8%).
**광고가 유발할 금액의 추정치가 아니다** — 전환율은 측정된 적이 없다.

> 관광소비 연결률로 C를 깎는 안은 검정이 지지하지 않았다. 연결분(ΔR² +0.018)보다
> 비연결분(+0.039)이 파급효과를 더 설명한다. 그래서 C의 정의는 그대로 뒀다.

---

## 4. 해외확산도 — 보조지표

```
축소 해외점유(f) = ( 해외언급 + α_L · m ) ÷ ( 총언급 + α_L )
                   α_L = 6.93,  m = 0.4201

G(f) = 축소 해외점유 ÷ 0.7763                        0.7763 = 표본 최대 (Stray Kids)
```

**가중치가 0.150 → 0.014로 내려간 항이다.** 근거문장수를 통제하면 언어지표와 파급효과의
편상관이 −0.15 ~ −0.23으로 사라진다 — 부호까지 뒤집힌다. 상한 0.014는 상업유발도와의
증분 설명력 비(0.049 : 0.008)로 나눈 값이고, 페르소나별로는 측정 해외비중에 비례한다.

| 페르소나 | 측정 해외비중 | w_L |
|---|---|---|
| 글로벌투어형 | 0.509 | 0.0140 |
| 원정소비형 | 0.491 | 0.0135 |
| 현장상업형 | 0.321 | 0.0089 |
| 집단동원형 | 0.190 | 0.0053 |

이 지표가 실제로 답하는 것은 규모가 아니라 **구성**이다 — 해외 13개 언어에 디리클레(α=5.93)를
적용한 축소 점유 상위 3종이 상품 명세의 다국어 대응 우선순위로 나간다.
1순위가 영어가 아닌 팬덤이 100개 중 8개다.

이름을 '언어적합도'에서 바꾼 이유: 지역 쪽 대응항이 없어 적합도가 아니다.
지역별 외래객 국적 구성을 확보해야 `팬덤 언어 프로파일 × 지역 외래객 구성`의 유사도로 승격된다.

---

## 5. 상품 설계 — 소비성향 배분

후보를 고른 다음, 일정 예산을 소비성향대로 나눈다.

```
소비영역 배분 ∝ 페르소나 템플릿 × √( 아티스트 광고 업종 LQ × 지역 소비구조 LQ )
```

수요 측(페르소나 × 아티스트)과 공급 측(지역 소비구조)을 나눠 쓴다.
스코어링은 수요 측만 보고 지역을 평가하고, 동선 설계는 공급 측까지 곱해
**그 지역이 실제로 흡수할 수 있는 업종**으로 시간을 배분한다.
기하평균인 이유는 두 LQ를 곱하면 제곱 스케일이 되어 한 영역이 배분을 독점하기 때문이다.

- **숙박**은 POI 예산에 넣지 않는다. 박 수로 실현되므로 이중계상이 된다.
  대신 체류일수를 산정한다: `템플릿 박수 × (실측 숙박 가중 ÷ 템플릿 숙박 가중)`
- **관광/쇼핑** 내부 4종(역사문화·자연경관·힐링체험·숙박로컬체험) 배분만 페르소나 템플릿을 쓴다 —
  소비 데이터로는 어느 쪽 관광인지 가를 수 없는 구간이다.
- 지역 LQ는 3.0에서 절단한다. 표본이 얇은 영역(레저활동은 LQ가 27까지 튄다)이 배분을 독점하지 않게.

일정은 카테고리 순서가 아니라 **배분 대비 부족분이 큰 유형부터** 채우고,
각 유형에서 남은 배분에 가장 가까운 길이의 장소를 고른다.
등록 관광지 재고를 넘는 목표는 잘라 나머지에 재배분하고, 그러고도 남는 예산은 **잔여**로 따로 표시한다 —
"이 지역에 이 상품 유형이 요구하는 업종의 관광지가 부족하다"는 신호가 묻히지 않도록.

---

## 버전 이력

| 버전 | 바뀐 것 |
|---|---|
| v0.2 | 연고밀착도 0/1 이진값 → 계단값 (0.90 / 0.70) |
| v0.3 | 4항 곱 구조 (점유 × 지리귀속 × 근거두께 × 지역밀착) |
| v0.4 | 검출지역 기대값 — 검출 지역 목록을 모른다는 전제의 3중 구조 |
| v0.5 | **실측 행렬 도입.** 팬덤 × 17개 시도 언급 행렬이 들어오며 추정 로직 전부 제거. 베이즈 축소 α=10 |
| v0.6 | **언어지수 검정 → 보조지표 강등** (0.150 → 0.080). 연고 사전분포를 비대칭 디리클레 경험베이즈로 교체, 척도를 지역언급 LQ로 재정의. 서울 대표 76 → 3 |
| v0.7 | **광고지수 검정 → 상업유발도 신설** (0.086). 업종적합도를 지역 × 아티스트로 확장. 해외확산도 ΔR² 기준 재조정 (0.080 → 0.014) |
| — | 상품 설계를 소비성향 배분으로 전환. 공연 이력을 실연 채널로 지역지수에 결합. 광고효과를 카드소비 금액으로 환산 |

각 개정의 근거와 수치는 `web/reports/` 의 v0.5 ~ v0.7 보고서에 있다.
